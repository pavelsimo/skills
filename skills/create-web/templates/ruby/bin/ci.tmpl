#!/usr/bin/env bash
set -e

cd "$(dirname "$0")/.."

echo "==> Linting (rubocop)..."
bundle exec rubocop

echo "==> Running tests..."
bin/rails test

echo "==> Running system tests..."
bin/rails test:system

echo ""
echo "✅ All checks passed!"
